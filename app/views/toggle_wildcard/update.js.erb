$('#add-trailing-wildcard').val('<%= session[:add_trailing_wildcard].to_s %>');
<% if session[:add_trailing_wildcard] %>
  $('#toggle-wildcard-link-tick').removeClass('hidden-xs-up');
<% else %>
  $('#toggle-wildcard-link-tick').addClass('hidden-xs-up');
<% end %>
window.hideAbout();
