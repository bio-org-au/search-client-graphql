<h4 class="form-heading"> </h4>
<ul class="plain">
  <li>
    <strong>List names</strong>
    <select name="format" title="Show the results in this form" class="form-field">
      <%= options_for_select([['on this page', 'html',{title:"Show results as a web page here"}],
                              ['CSV download', 'csv',{title:"Show results as comma-separated data."}],
                              ['JSON', 'json',{title:"Show results as JSON"}]],
                             params[:format]) %>
    </select>
  </li>
  <li>
    for up to
    <input type="number"
           name="limit"
           title="Limit the number of names retrieved"
           placeholder="records"
           min="1"
           max="5000"
           step="1"
           value="<%= params[:limit] || 100 %>"
           class="form-field"> records
  </li>
<h4 class="form-heading">
  Also show
</h4>
  <li>
    <%= check_box_tag(:show_family, "show", @client_request.family?,
                      title:
                      'Tick to show the family of each name next query') %>
    <%= label_tag(:show_family, "family name",
                  title: 'Tick to show the family of each name next query') %>
    <br/>
    <%= check_box_tag(:show_links, "show", @client_request.links?,
                      title: "Tick to turn names into links next query.") %>
    <%= label_tag(:show_links, "links to related names",
                  title: "Tick to turn names into links next query.") %>
    <br/>
    <%= check_box_tag(:show_details, "show", @client_request.details?,
                      title: "Tick to show details next query.") %>
    <%= label_tag(:show_details, "bibliographic details and synonyms",
                  title: "Tick to show details next query.") %>
  </li>
</ul>

