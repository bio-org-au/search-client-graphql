<% unless @results.paginator_info.nil? %>
  <% if @results.paginator_info.current_page.to_i > 1 || @results.paginator_info.total.to_i > @client_request.limit.to_i %>
    <%= first_ %>
    <%= previous %>
    <%= page %>
    <%= next_ %>
    <%= last_ %>
    <hr>
  <% end %>
<% end %>

