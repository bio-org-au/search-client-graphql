<%= link_to("#{fa_icon('pencil')}".html_safe,
                        "#{Rails.configuration.try('instance_editor')}/search?query_target=Names&query_string=id:#{name.id}",
                        class: "opens-in-new-tab-or-window for-editor gray",
                        target: "_blank",
                        title: "Edit this record in a new tab or window."
                       )
            %>
