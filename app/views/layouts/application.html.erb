<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title || 'Search' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <script>
    window.relative_url_root = "<%= Rails.application.config.relative_url_root %>";
  </script>
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.2.0/css/all.css" integrity="sha384-hWVjflwFxL6sNzntih27bfxkr27PmbbK/iSvJ+a4+0owXq79v+lsFkW54bOGbiDQ" crossorigin="anonymous">
  <style media="print">
     body {color:#000000;
           background-color:#FFFFFF;} 
     .no-print {display: none;}
  </style>
      <% if development? %>
<style>
a:not([title]),
select:not([title]),
textarea:not([title]),
input:not([title]) {
  background: red;
}

input[type="checkbox"]:not([title]),
label:not([title]) {
  background: purple;
  color: red;
  border: 2px solid black;
  width: 16px;
  height: 16px;
}
</style>

    <% end %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body class="xcolorize <%= @body_class %> <%= Rails.configuration.try('environment') %>" >

  <%= yield %>

<span id="editor-switch-container">
  <%= render partial: 'editor_switch' %>
</span>

<br>
version: <%= Rails.configuration.version %>;<% rte = RuntimeEnvironment.new %>  database: <%= rte.database %>; env: <%= rte.rails_env %>;
<br>
</body>
</html>
