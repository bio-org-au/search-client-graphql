<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title || 'Search' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <script>
    window.relative_url_root = "<%= Rails.application.config.relative_url_root %>";
  </script>
  <style media="print">
     body {color:#000000;
           background-color:#FFFFFF;} 
     .no-print {display: none;}
  </style>
      <% if development? %>
<style>
a:not([title]),
select:not([title]),
textarea:not([title]),
input:not([title]) {
  background: red;
}

input[type="checkbox"]:not([title]),
label:not([title]) {
  background: purple;
  color: red;
  border: 2px solid black;
  width: 16px;
  height: 16px;
}
</style>

    <% end %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body class="<%= @body_class %> <%= Rails.configuration.try('environment') %>" >

<%= yield %>

<span id="show-edit-links-area">
<% if session[:show_edit_links] %>
  <%= link_to('Edit Links off',
              update_prefs_path(switch: 'edit-links', on_off: 'off'),
              id: "link-to-turn-off-edit-links",
              title: "Click to stop showing links to the NSL Editor.",
              remote: true)  %>
<% end %>
</span>
</body>
</html>
