<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title || 'Search' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <script>
    window.relative_url_root = "<%= Rails.application.config.relative_url_root %>";
  </script>
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.2.0/css/all.css" integrity="sha384-hWVjflwFxL6sNzntih27bfxkr27PmbbK/iSvJ+a4+0owXq79v+lsFkW54bOGbiDQ" crossorigin="anonymous">
  <style media="print">
     body {color:#000000;
           background-color:#FFFFFF;} 
     .no-print {display: none;}
  </style>
      <% if development? %>
<style>
a:not([title]),
select:not([title]),
textarea:not([title]),
input:not([title]) {
  background: red;
}

input[type="checkbox"]:not([title]),
label:not([title]) {
  background: purple;
  color: red;
  border: 2px solid black;
  width: 16px;
  height: 16px;
}
</style>

    <% end %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= @background_class %> <%= @body_class %> <%= Rails.configuration.try('environment') %>" >

<div class='help-bar'>

  <span class="env"><%= Rails.configuration.try('environment') %> </span>

  <select id="colour-scheme-picker" class="form-field no-print" title="Choose a colour scheme">
    <option value="light" <%= 'selected' if session[:background] == 'light' %>>Light Background</option>
    <option value="dark" <%= 'selected' if session[:background] == 'dark' %>>Dark Background</option>
  </select>

  <a href="" id="general-help-link" title="Show helpful information about the page." >help</a>
</div>

  <%= yield %>

<span id="editor-switch-container" class="no-print">
  <%= render partial: 'editor_switch' %>
</span>

<br>
</body>
</html>
