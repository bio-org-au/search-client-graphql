<span class="debug red hidden-print">[plants/names/search/within/taxon/selected_ranks/_search]</span>
<%= render partial: '/plants/names/search/form' %>

<% if @search.present? %>
  <%= "<span class='search-result-summary'>#{pluralize(@search.results.size, 'scientific name')}</span>".html_safe %>
<% end %>

<% if @search.present? %>
  <%= render partial: '/plants/names/search/results', locals: {name: @search.results.first} %>
<% end %>
