<table class="name-check">
  <tr>
    <th class="left-align">Search Term</th>
    <th class="left-align">Matching Name</th>
    <th class="left-align">Matching Name Family</th>
  </tr>
<% @wrapped_search_results.results.each do |result| %>
  <tr>
    <td><%= result.search_term unless result.index.to_i > 1 %></td>
    <% if result.found? %>
      <td><%= link_to(result.matched_name_full_name, show_name_path(id: result.matched_name_id, search_form: 'names'), title: 'Show name') %></td>
    <% else %>
      <td></td>
    <% end %>
    <% if result.found? && result.matched_name_family_name.present? %>
      <td><%= link_to(result.matched_name_family_name, show_name_path(id: result.matched_name_family_name_id, search_form: 'names'), title: 'Show name') %></td>
    <% else %>
      <td></td>
    <% end %>
  </tr>
<% end %>
</table>

